# MediaPipe bootstrap summary

## Initial attempt commands run
1. `bd update openclaw-byte-qrz --status in_progress --json`  
   Outcome: succeeded; bead claimed in `/home/derrick/.openclaw/workspace/projects/openclaw-byte`.
2. `grep -RInE "godotenv|prepare_runtime|install|update|restore|addon|requirements|sidecar|python_mediapipe" README.md AGENTS.md CLAUDE.md docs scripts python_mediapipe 2>/dev/null | head -n 250`  
   Outcome: succeeded; confirmed the documented Linux runtime prep command and separate GodotEnv restore step in `README.md` and `docs/proving-scene-human-verification-checklist.md`.
3. `python3 python_mediapipe/prepare_runtime.py --platform linux-x64 --mode dev --install-requirements --validate`  
   Outcome: failed.
4. `cd .testbed && godotenv addons install`  
   Outcome: succeeded.
5. `python_mediapipe/assets/runtimes/linux-x64/venv/bin/python --version && (python_mediapipe/assets/runtimes/linux-x64/venv/bin/python -m pip --version || true)`  
   Outcome: Python executable exists and reports `Python 3.12.3`, but `pip` is unavailable inside the runtime venv.

## Retry commands run
1. `~/.openclaw/workspace/scripts/check-sudo.sh`  
   Outcome: succeeded; temporary sudo grant `20260511T115702Z-derrick-6ac7d05b` was active.
2. `python3 -m venv "$(mktemp -d)/venv"`  
   Outcome: failed before install with the standard Debian/Ubuntu `ensurepip is not available` message recommending `apt install python3.12-venv`.
3. `sudo -n apt-get install -y python3.12-venv`  
   Outcome: succeeded; installed `python3.12-venv` plus the required wheel payload packages `python3-pip-whl` and `python3-setuptools-whl`.
4. `python3 -m venv "$(mktemp -d)/venv" && <tmp>/venv/bin/python -m pip --version`  
   Outcome: succeeded; confirmed host Python can now create venvs with working `pip`.
5. `python3 python_mediapipe/prepare_runtime.py --platform linux-x64 --mode dev --install-requirements --validate`  
   Outcome: still failed because the existing runtime-local venv was reused and still lacked `pip`.
6. `python3 python_mediapipe/prepare_runtime.py --platform linux-x64 --mode dev --install-requirements --force --validate`  
   Outcome: succeeded; recreated the runtime-local venv, installed `python_mediapipe/requirements.txt`, and wrote the runtime contract files.
7. `test -f python_mediapipe/assets/runtimes/linux-x64/.runtime-ready && test -f python_mediapipe/assets/runtimes/linux-x64/runtime-manifest.json && python_mediapipe/assets/runtimes/linux-x64/venv/bin/python -m pip --version`  
   Outcome: succeeded; sentinel and manifest exist, and runtime `pip` works.

## Python runtime prep
- **Succeeded:** Yes, after installing the host venv prerequisite and rerunning with `--force` to replace the stale partial runtime venv.
- **Canonical command used on success:** `python3 python_mediapipe/prepare_runtime.py --platform linux-x64 --mode dev --install-requirements --force --validate`
- **Host prerequisite installed:** `python3.12-venv`
- **Observed prepared state:**
  - `python_mediapipe/assets/runtimes/linux-x64/venv/bin/python` exists and reports `Python 3.12.3`
  - `python_mediapipe/assets/runtimes/linux-x64/venv/bin/python -m pip --version` succeeds
  - `python_mediapipe/assets/runtimes/linux-x64/.runtime-ready` exists
  - `python_mediapipe/assets/runtimes/linux-x64/runtime-manifest.json` exists
  - `runtime-manifest.json` reports `validation_status: "ready"`

## GodotEnv addon restore
- **Succeeded:** Yes
- **Canonical command used:** `cd .testbed && godotenv addons install`
- **Observed restored mounts:**
  - `.testbed/addons/aerobeat-input-mediapipe-python`
  - `.testbed/addons/aerobeat-input-core`
  - `.testbed/addons/gut`

## Notes
- The minimal host fix was `sudo -n apt-get install -y python3.12-venv`.
- Because the first failed prep left behind a partial runtime venv without `pip`, the documented retry needed `--force` once to recreate that venv cleanly.
